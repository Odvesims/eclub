<% if !@clubesarchivo.errors.empty? %>
   <div class = "alert alert-error">
      
      <ul>
         <% @clubesarchivo.errors.full_messages.each do |msg| %>
            <li><%= msg %></li>
         <% end %>
      </ul>
      
   </div>
<% end %>

<div class = "well">
   <%= form_for @clubesarchivo, html: { multipart: true } do |f| %>
      <%= f.label :nombre %>
      <%= f.text_field :nombre %>
      <%= f.label :attachment %>
      <%= f.file_field :attachment %>
      <%= f.submit "Save", class: "btn btn-primary" %>
   <% end %>
</div>