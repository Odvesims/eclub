require 'rubygems'
require 'json'
require 'net/http'
require 'date'
require 'socket'
 
class SocketApiController < ActionController::Base 
  respond_to :json 
	def index
		start = "@"
		divider = "|"
		end = "?"
		s = TCPSocket.new 'localhost', 60000
		s.puts start + option + request_str + end
		while line = s.gets # Read lines from socket
			puts line         
		end
		s.close
	end
end